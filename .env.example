# Supabase — Project Settings → API
# Only the PUBLIC (publishable/anon) key is needed. No secret key in a static build.
# Admin login uses Supabase Auth (Dashboard → Authentication → Users).
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=your_publishable_or_anon_key
